---
name: Go To Earliest Menu
overview: Add a **Go to** menu with an **Earliest** action in `MainTimelineWindow.ui`, and wire it in `MainTimelineWindow.py` to scroll the embedded `SimpleTimelineWidget` (when present) to `total_data_start_time` via the existing `simulate_window_scroll` API.
todos:
  - id: ui-menu
    content: Add Go to menu + Earliest action + QAction in MainTimelineWindow.ui
    status: completed
  - id: py-wire
    content: Connect actionGoToEarliest in MainTimelineWindow.initUI; call simulate_window_scroll(total_data_start_time)
    status: completed
isProject: false
---

# Add "Go to → Earliest" to MainTimelineWindow

## Context

- [MainTimelineWindow.ui](c:\Users\pho\repos\EmotivEpoc\ACTIVE_DEV\pyPhoTimeline\pypho_timeline\widgets\TimelineWindow\MainTimelineWindow.ui): `menubar` exists but has no menus (lines 139–148).
- [MainTimelineWindow.py](c:\Users\pho\repos\EmotivEpoc\ACTIVE_DEV\pyPhoTimeline\pypho_timeline\widgets\TimelineWindow\MainTimelineWindow.py): `initUI()` already wires footer controls; `timeline_widget` returns the first child of `contentWidget`’s layout (lines 64–71).
- [simple_timeline_widget.py](c:\Users\pho\repos\EmotivEpoc\ACTIVE_DEV\pyPhoTimeline\pypho_timeline\widgets\simple_timeline_widget.py): `total_data_start_time` is the global data start; `simulate_window_scroll(new_start_time)` keeps the current window duration and updates plots, overview strip, and `window_scrolled` (lines 377–417) — same pattern as interval jump buttons (lines 327–336).

## 1. UI: `MainTimelineWindow.ui`

- Under `QMenuBar` `menubar`, add a child `QMenu` (e.g. `menuGoTo`) with title **Go to**.
- Add `addaction` for a new `QAction` (e.g. `actionGoToEarliest`) with text **Earliest**.
- As a sibling to `centralwidget` / `menubar` (standard Qt Designer placement), define the `QAction` with `name="actionGoToEarliest"` and `text` **Earliest**.

PyQt’s `uic.loadUi` will expose `self.actionGoToEarliest` and `self.menuGoTo` on the window instance.

## 2. Python: `MainTimelineWindow.py`

- In `initUI()`, connect `self.actionGoToEarliest.triggered` to a small handler (e.g. `_on_go_to_earliest`).
- Handler logic (duck-typed so an empty `MainTimelineWindow` still loads):
  - `tw = self.timeline_widget`
  - If `tw` is `None`, return.
  - If `tw` has `simulate_window_scroll` and `total_data_start_time`, call `tw.simulate_window_scroll(tw.total_data_start_time)`.

No changes to `timeline_builder` or `SimpleTimelineWidget` are required unless you later want shortcuts or to move compare view in lockstep (not requested).

## Notes

- **Standalone window** (`python -m ... MainTimelineWindow` with no timeline): action is a no-op; optional later improvement is `setEnabled(False)` when there is no capable widget.
- File header comment says the `.py` is generated by an extension; if you rely on regen, duplicate the menu/handler after regen or adjust the generator — otherwise editing [MainTimelineWindow.py](c:\Users\pho\repos\EmotivEpoc\ACTIVE_DEV\pyPhoTimeline\pypho_timeline\widgets\TimelineWindow\MainTimelineWindow.py) directly is consistent with current project practice (footer wiring already lives there).

